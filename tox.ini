[tox]
envlist = py36

[testenv]
deps = pytest
       coverage
commands =  pytest
    coverage erase
    coverage run --branch __main__.py test
    coverage report --omit='.tox/*,venv/*'
    coverage html --omit='.tox/*,venv/*'


